// config.js
const IP = '192.168.10.159';
const PORT = '8000';

export const API_BASE_URL = `http://${IP}:${PORT}`;
export const API_INCIDENTS_URL = `http://${IP}:${PORT}/api/incidents`;
export const API_DETECTION_URL = `http://${IP}:${PORT}/api/detection`;